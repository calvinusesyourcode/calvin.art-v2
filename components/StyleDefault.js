export function StyleDefault(){
    return (
                <style> {`
                    html, body {
                        background-color: var(--color-bg);
                        color: var(--color-text);
                    }
                `}</style>
    )
}